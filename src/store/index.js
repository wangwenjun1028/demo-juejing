import Vue from 'vue'
import Vuex from 'vuex'
import { store as topic } from '../topic/store'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    topic
  }
})
